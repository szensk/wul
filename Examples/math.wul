; math.wul
; std lib definitions 
(using System.Math)

(defn cos (n) (:: Cos n))
(defn sin (n) (:: Sin n))
(defn tan (n) (:: Tan n))
(defn abs (n) (:: Abs n))
(defn sqrt (n) (:: Sqrt n))
(defn max (a b) (:: Max a b))
(defn min (a b) (:: Min a b))
(defn round (n) (:: Round n))
(defn floor (n) (:: Floor n))
(defn ceil (n) (:: Ceiling n))
(defn log (n) (:: Log n))
(defn log10 (n) (:: Log10 n))

(print "loaded math.wul")